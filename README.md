# mdhouse
run mdhouse ~/git and get every .md file under that directory as a browsable, searchable, editable site in your browser. Tick checkboxes and they're written back to disk; see what changed via git; select a section and push it to Claude or Codex for feedback
